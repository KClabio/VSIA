<span class="logo-text">
  <% if (typeof siteLogo !== 'undefined' && siteLogo) { %>
    <img src="<%= siteLogo %>" alt="VSIA" class="site-logo-img">
  <% } else { %>
    <span class="wm">VSIA</span><span class="tag">Giáo dục STEM</span>
  <% } %>
</span>
